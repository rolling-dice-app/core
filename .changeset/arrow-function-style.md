---
'@rolling-dice-app/core': patch
---

refactor(defaults): 將 `createDefaultArmorClass` / `createDefaultInventory` / `buildCharacterCreateDefaults` 從 function declaration 改為 arrow function expression，統一程式風格。執行語意不變。
